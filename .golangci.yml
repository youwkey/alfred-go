linters:
  enable-all: true
  disable:
    - scopelint
    - interfacer
    - maligned
    - golint
issues:
  exclude-use-default: false
  exclude-rules:
    - path: _test\.go
      linters:
        - funlen
